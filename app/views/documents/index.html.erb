<script type="text/javascript">
$(function(){ 
	
	
	
	var $pdfInput = $("#pdf-location");
	var $response = $('#response');
	var formData = false;

	var docs = <%= Document.all.to_json.html_safe %>; 
	
	window.PT.initialize($('#doc-list'), $('#pdf-reader'), $('#tag-listing'), docs);

	
	function showUploadedItem(title) {
		var $list = $("pdf-list");
		var $li = $('<li></li>');
		$li.append(title);
		$list.append($li);
	}
	
	$pdfInput.change(function(evt){
	
		console.log($pdfInput);
		var file = $pdfInput[0].files[0];
		var reader = new FileReader();
		reader.readAsDataURL(file);
		reader.onload = shipOff;
		
	});
	
	function shipOff(event) {
	    var result = event.target.result;
	    var fileName = $pdfInput[0].files[0].name;
		$.post(
			'/documents.json',
			 {
				 document:  {title: fileName,
					      pdf_data: result }
	});
	}
	
	// $('#set-selection').click(function() {
	// 	window.Selection.Status = true;
	// 	
	// });
	
}); 
</script>
<div class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <a class="brand" href="#">PDFtagger</a>
    <ul class="nav">
      <li class="active"><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
      <li><a href="#" id="set-selection">Add Selection</a></li>
    </ul>
  </div>
</div>

<div class="row" id="main-content">
  <div class="span4">
	  <h1>Doc List</h1><br>
  	  <!-- <% Document.all.each do |doc| %>
	  
	  <%= doc.title %><br>
	  
  	  <% end %> -->
		  
		  <div id='doc-list'></div>
  	
	  <br>
	  <div class=container>

			<form action="/documents" method="post">
				
				<label for="pdf-location">Upload File</label>
				<input type="file" name="document[pdf]" id="pdf-location" value="upload pdf" target="upload_frame" multiple/>
				
				<br>
				<button id="start-upload">Upload</button>
			</form>
			<iframe id='upload_frame' name="upload_frame" style="width:1px;height:1px;border:0px" src="about:blank"></iframe>
			<div id="response"></div>
			<ul id="pdf-list">
			</ul>
			
		</div>
  </div>
  <div class="span12 offset-2">
	  <h1>Pdf Reader</h1>
	  <div id="pdf-reader" class="well"></div>
  
  </div>
  <div class="span2 offset2">
	   <h1>Tags</h1>
	   <div id="tag-listing"></div>
  </div>
</div>

